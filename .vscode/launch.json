{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug(custom)",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/xampp/htdocs/project/assignment": "${workspaceFolder}"
            }
        }
    ]
}